{
  "name": "length-prefixed-buffers",
  "version": "1.0.0",
  "description": "encode and decode an array of buffers as a single varint length-prefixed binary blob",
  "main": "index.js",
  "dependencies": {
    "varint": "^6.0.0"
  },
  "devDependencies": {
    "tape": "^5.2.2"
  },
  "scripts": {
    "test": "tape test/*.js"
  },
  "keywords": [
    "buffer",
    "uint8array",
    "array",
    "varint",
    "length-prefixed"
  ],
  "license": "bsd"
}
